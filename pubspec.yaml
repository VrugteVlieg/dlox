name: dlox
description: An implementation of the lox programming language.
version: 1.0.0
repository: https://github.com/VrugteVlieg

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  logging: ^1.3.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.24.0
